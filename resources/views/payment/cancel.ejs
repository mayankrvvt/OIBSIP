<section class="py-20 bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="bg-white p-10 rounded-2xl shadow-2xl max-w-xl w-full text-center animate-fade-in">
    <div class="flex justify-center mb-6">
      <div class="h-20 w-20 rounded-full bg-red-100 flex items-center justify-center">
        <svg class="h-12 w-12 text-red-600" fill="none" stroke="currentColor" stroke-width="2"
          viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round"
            d="M6 18L18 6M6 6l12 12" />
        </svg>
      </div>
    </div>
    <h1 class="text-3xl md:text-4xl font-bold text-red-700 mb-4">Payment Cancelled</h1>
    <p class="text-gray-600 text-lg mb-6">Your payment was not completed. Please try again from the cart.</p>
    <a href="/cart" class="inline-block bg-red-600 hover:bg-red-700 text-white px-6 py-3 rounded-full font-semibold transition duration-300">
      Back to Cart
    </a>
  </div>
</section>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.6s ease-out both;
  }
</style>
